# Configurações para PRODUÇÃO no Google Cloud
# Copie este arquivo para .env e ajuste os valores

# ============================================
# CLOUD SQL - Banco de Dados
# ============================================
# Opção 1: IP Privado do Cloud SQL (recomendado)
DB_HOST=10.123.45.67

# Opção 2: Connection Name (se usar Cloud SQL Proxy)
# DB_HOST=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME

DB_PORT=5432
DB_NAME=terafy_db
DB_USER=postgres
DB_PASSWORD=sua-senha-super-segura-do-cloud-sql

# SSL obrigatório em produção
DB_SSL_MODE=require

# ============================================
# SERVIDOR
# ============================================
SERVER_PORT=8080

# ============================================
# JWT - SEGURANÇA
# ============================================
# IMPORTANTE: Use Secret Manager do Google Cloud em produção!
# Gere uma chave segura: openssl rand -base64 64
JWT_SECRET_KEY=sua-chave-secreta-super-segura-aqui-mude-em-producao
JWT_EXPIRATION_DAYS=7

# ============================================
# NGINX (opcional)
# ============================================
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

