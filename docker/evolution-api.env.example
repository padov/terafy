# Evolution API - Configuração de Teste
# Copie este arquivo para evolution-api.env e ajuste os valores

# Chave de autenticação da API (OBRIGATÓRIO - mude em produção!)
EVOLUTION_API_KEY=terafy-test-key-change-me

# URL do servidor (para webhooks)
SERVER_URL=http://localhost:8080

# URL do webhook do Terafy (ajuste conforme necessário)
# Em desenvolvimento local, use host.docker.internal para acessar o host
# Em produção, use a URL pública do servidor
WEBHOOK_URL=http://host.docker.internal:8080/whatsapp/webhook

# Configurações opcionais
# DATABASE_ENABLED=false
# REDIS_ENABLED=false

